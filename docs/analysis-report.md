# 기존 웹 프로젝트 분석 및 로컬 전용 앱 전환 설계

## 1. 기존 프로젝트 분석

### 1.1 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | Sister Hair Salon Reservation System |
| **타입** | 풀스택 웹 애플리케이션 (클라이언트-서버 분리형) |
| **목적** | 헤어 살롱 예약 관리 시스템 |
| **완성도** | 100% (프로덕션 준비 완료) |

### 1.2 기술 스택

#### 프론트엔드 (salon-reservation-client)
| 기술 | 버전 | 용도 |
|------|------|------|
| React | 19.1.0 | UI 라이브러리 |
| TypeScript | 4.9.5 | 정적 타입 체크 |
| Tailwind CSS | - | 스타일링 (글라스모피즘 디자인) |
| Axios | 1.6.2 | HTTP 클라이언트 |
| Recharts | 3.1.2 | 차트/데이터 시각화 |
| React Calendar | 6.0.0 | 달력 컴포넌트 |
| date-fns | 4.1.0 | 날짜 처리 |

#### 백엔드 (salon-reservation-server)
| 기술 | 버전 | 용도 |
|------|------|------|
| Node.js | - | 런타임 환경 |
| Express.js | 4.21.2 | 웹 프레임워크 |
| SQLite 3 | - | 관계형 데이터베이스 |
| better-sqlite3 | 12.2.0 | SQLite 드라이버 |
| jsonwebtoken | 9.0.2 | JWT 인증 |
| bcryptjs | 3.0.2 | 비밀번호 해싱 |

### 1.3 주요 기능 목록

#### 예약 관리
- 예약 CRUD (생성/조회/수정/삭제)
- 5단계 예약 상태 관리 (대기/확정/완료/취소/노쇼)
- 상태 변경 이력 추적
- 대화형 캘린더로 예약 현황 시각화
- 고급 검색 및 필터링 (고객명/디자이너/서비스/상태별)

#### 디자이너 관리
- 디자이너 등록/수정/삭제
- 전문분야, 활성상태 관리
- 예약 시 활성화된 디자이너만 선택 가능

#### 영업시간 관리
- 요일별 영업시간 설정 (시작/종료, 휴게시간)
- 특별 영업시간 및 임시 휴무
- 한국 공휴일 API 연동 자동 휴일 관리

#### 통계 대시보드
- 종합 통계 (총 예약수, 일평균, 가장 바쁜 날/시간)
- 다양한 차트 시각화 (라인, 바, 파이)
- 기간별 분석 (7일/30일/90일)
- 요일별/시간대별 히트맵

#### 사용자 인터페이스
- 글라스모피즘 UI 디자인
- 반응형 레이아웃 (모바일/태블릿/데스크탑)
- 다크/라이트 모드
- 키보드 접근성

#### 인증 시스템
- JWT 토큰 기반 관리자 인증
- 세션 관리

### 1.4 데이터베이스 구조

```
8개 주요 테이블:
├── administrators     # 관리자 계정
├── reservations       # 예약 정보
├── hair_designers     # 디자이너 정보
├── business_hours     # 영업시간
├── holidays           # 휴일
├── special_hours      # 특별 영업시간
├── reservation_status_history  # 예약 상태 이력
└── customers          # 고객 정보
```

### 1.5 API 엔드포인트 구조

| 분류 | 엔드포인트 | 인증 필요 |
|------|-----------|----------|
| 인증 | `/api/auth/*` | 일부 |
| 예약 | `/api/reservations/*` | 상태 변경만 |
| 디자이너 | `/api/designers/*` | CUD 작업 |
| 영업시간 | `/api/business-hours/*` | CUD 작업 |
| 통계 | `/api/statistics/*` | 전체 |

---

## 2. 로컬 전용 앱 전환 설계

### 2.1 전환 목표

1. **서버 의존성 제거**: Node.js 백엔드 서버 없이 독립 실행
2. **데이터 로컬 저장**: 사용자 기기에 데이터 저장
3. **오프라인 지원**: 인터넷 연결 없이 완전한 기능 제공
4. **배포 간소화**: 단일 실행 파일 또는 설치 패키지
5. **멀티 플랫폼**: Desktop (macOS/Windows/Linux) + Mobile (iOS/Android)

### 2.2 앱 프레임워크 선택지

| 프레임워크 | 장점 | 단점 | 권장도 |
|-----------|------|------|--------|
| **Tauri 2.0** | 경량, Rust 보안, **Mobile 지원** | Rust 학습 필요 | ⭐⭐⭐⭐⭐ |
| **Electron** | 웹 기술 그대로, 풍부한 생태계 | 무거움 (150MB+), Desktop만 | ⭐⭐⭐ |
| **React Native** | Mobile 전용, 풍부한 생태계 | Desktop 미지원 | ⭐⭐⭐ |

**권장**: **Tauri 2.0** - Desktop + Mobile 모두 지원, 경량화와 보안 우수

### 2.3 멀티 플랫폼 지원

#### 지원 플랫폼
| 플랫폼 | 지원 | 빌드 타겟 |
|--------|------|-----------|
| **macOS** | ✅ | `tauri build --target universal-apple-darwin` |
| **Windows** | ✅ | `tauri build --target x86_64-pc-windows-msvc` |
| **Linux** | ✅ | `tauri build --target x86_64-unknown-linux-gnu` |
| **iOS (iPad/iPhone)** | ✅ | `tauri ios build` |
| **Android (태블릿/폰)** | ✅ | `tauri android build` |

#### Tauri Mobile 설정
- `tauri.conf.json`에 iOS/Android 타겟 추가
- 모바일용 권한 설정 (파일 접근, iCloud, 카메라 등)
- `src-tauri/gen/` 디렉토리에 네이티브 프로젝트 생성

### 2.4 반응형 UI 설계

#### 브레이크포인트
| 디바이스 | 너비 | 레이아웃 |
|----------|------|----------|
| **모바일 (iPhone)** | < 640px | 단일 컬럼, 바텀 탭 네비게이션 |
| **태블릿 (iPad)** | 640-1024px | 2컬럼, 접이식 사이드바 |
| **데스크탑** | > 1024px | 3컬럼, 고정 사이드바 |

#### 적응형 컴포넌트
| 컴포넌트 | 데스크탑 | 모바일 |
|----------|----------|--------|
| **네비게이션** | 고정 사이드바 | 바텀 탭 |
| **캘린더** | 월간 + 일간 동시 표시 | 월간만, 스와이프 |
| **예약 테이블** | 전체 컬럼 테이블 | 카드 리스트 |
| **통계 대시보드** | 그리드 레이아웃 | 세로 스크롤 |

#### Tailwind 반응형 클래스 규칙
```css
/* 모바일 우선 설계 (기본 스타일 = 모바일) */
sm: (640px+)   /* 태블릿 세로 */
md: (768px+)   /* 태블릿 가로 */
lg: (1024px+)  /* 데스크탑 */
xl: (1280px+)  /* 대형 모니터 */
```

#### UI 레이아웃 미리보기

**iPhone (< 640px)**
```
┌─────────────────────┐
│  Sisters Salon  ☰   │
├─────────────────────┤
│   [  캘린더 뷰  ]   │
│   ┌─────────────┐   │
│   │ 예약 카드 1 │   │
│   └─────────────┘   │
│   ┌─────────────┐   │
│   │ 예약 카드 2 │   │
│   └─────────────┘   │
├─────────────────────┤
│ 📅  ➕  👨‍🎨  📊  ⚙️ │
└─────────────────────┘
```

**iPad (640-1024px)**
```
┌───────────────────────────────────────┐
│  Sisters Salon                    ☰   │
├─────────┬─────────────────────────────┤
│ 📅 예약 │     [   캘린더 뷰   ]       │
│ ➕ 새예약│  ┌─────────┬─────────┐      │
│ 👨‍🎨 디자│  │ 예약 1  │ 예약 2  │      │
│ 📊 통계 │  └─────────┴─────────┘      │
│  [접기] │                             │
└─────────┴─────────────────────────────┘
```

**Desktop (> 1024px)**
```
┌─────────────────────────────────────────────────────────┐
│  Sisters Salon Reservation                              │
├───────────┬─────────────────────────┬───────────────────┤
│ 📅 예약   │    [   캘린더 뷰   ]    │  오늘의 예약      │
│ ➕ 새예약 │  ┌───────┬───────┬────┐ │  10:00 김민재    │
│ 👨‍🎨 디자이│  │예약 1 │예약 2 │... │ │  11:00 이영희    │
│ 📊 통계   │  └───────┴───────┴────┘ │  14:00 박철수    │
│ ⚙️ 설정   │                         │                   │
└───────────┴─────────────────────────┴───────────────────┘
```

### 2.5 아키텍처 설계

#### 기존 (웹 앱)
```
┌─────────────┐    HTTP     ┌─────────────┐    SQL    ┌─────────────┐
│   React     │ ─────────►  │   Express   │ ────────► │   SQLite    │
│   Client    │ ◄─────────  │   Server    │ ◄──────── │   (Server)  │
└─────────────┘    JSON     └─────────────┘           └─────────────┘
```

#### 신규 (로컬 앱)
```
┌───────────────────────────────────────────────────────────────────┐
│                        Tauri Application                          │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │                     React Frontend                          │  │
│  │  (기존 컴포넌트 재사용 + API 호출 → 로컬 함수 호출로 변경)    │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                              │ IPC                                │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │                    Tauri Backend (Rust)                     │  │
│  │  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │  │
│  │  │ Commands    │    │  Services   │    │   SQLite    │     │  │
│  │  │ (API 대체)  │───►│ (비즈니스   │───►│ (로컬 파일) │     │  │
│  │  │             │    │  로직)      │    │             │     │  │
│  │  └─────────────┘    └─────────────┘    └─────────────┘     │  │
│  └─────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────┘
```

### 2.6 데이터 저장소 전략

#### 옵션 비교

| 저장소 | 용량 | 쿼리 성능 | 복잡도 | 권장 |
|--------|------|----------|--------|------|
| **SQLite (파일)** | 무제한 | 우수 | 중간 | ⭐⭐⭐⭐⭐ |
| IndexedDB | ~500MB | 양호 | 높음 | ⭐⭐⭐ |
| LocalStorage | 5-10MB | 낮음 | 낮음 | ⭐ |

**권장**: **SQLite 파일 기반** - 기존 스키마 그대로 사용 가능

#### 데이터 파일 위치
```
macOS: ~/Library/Application Support/sisters-salon-reservation/
Windows: %APPDATA%/sisters-salon-reservation/
Linux: ~/.config/sisters-salon-reservation/

구조:
├── database.db          # SQLite 데이터베이스
├── config.json          # 앱 설정
└── backups/             # 자동 백업
    └── backup_YYYYMMDD.db
```

### 2.7 기능 변환 매핑

#### 인증 시스템 변경

| 기존 (웹) | 신규 (로컬) |
|----------|------------|
| JWT 토큰 기반 | 로컬 PIN/비밀번호 (선택적) |
| 서버 세션 관리 | 앱 상태 관리 |
| API 인증 미들웨어 | 앱 잠금 화면 |

#### API → Tauri Commands 변환

```typescript
// 기존 (Axios)
const response = await axios.get('/api/reservations');

// 신규 (Tauri invoke)
const reservations = await invoke('get_reservations');
```

| 기존 API | Tauri Command |
|----------|---------------|
| `GET /api/reservations` | `get_reservations` |
| `POST /api/reservations` | `create_reservation` |
| `PUT /api/reservations/:id` | `update_reservation` |
| `DELETE /api/reservations/:id` | `delete_reservation` |
| `GET /api/designers` | `get_designers` |
| `GET /api/business-hours` | `get_business_hours` |
| `GET /api/statistics/*` | `get_statistics` |

### 2.8 컴포넌트 재사용 계획

#### 그대로 재사용 가능 (95%)
- `AppointmentForm.tsx` - API 호출부만 수정
- `ReservationTable.tsx` - API 호출부만 수정
- `Calendar.tsx` - 그대로 사용
- `DesignerManagement.tsx` - API 호출부만 수정
- `BusinessHours.tsx` - API 호출부만 수정
- `StatisticsDashboard.tsx` - API 호출부만 수정
- `SearchFilter.tsx` - 그대로 사용

#### 수정 필요
- `AuthContext.tsx` → `AppContext.tsx` (인증 → 앱 설정)
- `LoginForm.tsx` → `LockScreen.tsx` (옵션: PIN 잠금)

#### 신규 추가
- `src/lib/tauri.ts` - Tauri API 래퍼
- `src/hooks/useDatabase.ts` - 데이터베이스 훅

### 2.9 신규 기능 추가 (로컬 앱 장점 활용)

1. **자동 백업**: 매일 자동으로 데이터베이스 백업
2. **내보내기/가져오기**: JSON, CSV, Excel 형식 지원
3. **데이터 동기화**: 여러 기기 간 수동 동기화 (파일 기반)
4. **시스템 알림**: 예약 리마인더 (OS 네이티브 알림)
5. **자동 시작**: 시스템 시작 시 자동 실행 (선택)

### 2.10 Excel 내보내기 기능 상세 설계

#### 라이브러리
- **rust_xlsxwriter** - Rust 네이티브 Excel 파일 생성 라이브러리

#### 시트 구성

| 시트명 | 내용 | 컬럼 |
|--------|------|------|
| **예약목록** | 기간 내 전체 예약 | 날짜, 시간, 고객명, 연락처, 디자이너, 서비스, 상태 |
| **월별통계** | 월별 예약 현황 | 년월, 총 예약수, 완료, 취소, 노쇼, 매출추정 |
| **디자이너실적** | 디자이너별 성과 | 디자이너명, 예약수, 완료율, 평균 예약/일 |

#### 내보내기 기간 옵션
```typescript
type ExportPeriod = 'this_month' | 'last_3_months' | 'all';
```

| 옵션 | 설명 |
|------|------|
| `this_month` | 이번 달 데이터만 |
| `last_3_months` | 최근 3개월 데이터 |
| `all` | 전체 데이터 |

#### 플랫폼별 내보내기 방식
| 플랫폼 | 방식 |
|--------|------|
| **Desktop** | 파일 저장 다이얼로그 |
| **iOS** | 공유 시트 (Share Sheet) |
| **Android** | Intent로 공유 또는 다운로드 폴더 저장 |

#### Tauri Command
```rust
#[tauri::command]
pub async fn export_to_excel(
    period: ExportPeriod,
    output_path: Option<String>  // Desktop만, Mobile은 None
) -> Result<String, String> {
    // rust_xlsxwriter로 Excel 파일 생성
    // iOS/Android는 공유 시트로 처리
}
```

### 2.11 클라우드 백업 기능 상세 설계

#### 플랫폼별 클라우드 서비스

| 서비스 | 플랫폼 | 구현 방식 |
|--------|--------|----------|
| **iCloud** | iOS/macOS | NSUbiquitousKeyValueStore / iCloud Drive |
| **Google Drive** | Android/선택적 | Google Drive API + OAuth |

#### iCloud 백업 (iOS/macOS)
```
경로: iCloud Drive/SistersSalon/
구조:
├── backups/
│   ├── backup_2026-01-21.db
│   ├── backup_2026-01-20.db
│   └── ...
└── sync_metadata.json
```

#### Google Drive 백업 (Android/선택적)
- OAuth 2.0 인증 필요
- Google Drive API v3 사용
- 전용 앱 폴더에 백업 저장

#### 자동 백업 설정

| 설정 | 옵션 |
|------|------|
| **주기** | 매일 / 매주 / 수동 |
| **보관 기간** | 최근 7개 / 30개 / 무제한 |
| **백업 시간** | 앱 종료 시 / 지정 시간 |

#### 백업 관련 Tauri Commands
```rust
#[tauri::command]
pub async fn backup_to_cloud(service: CloudService) -> Result<BackupInfo, String>;

#[tauri::command]
pub async fn restore_from_backup(backup_id: String) -> Result<(), String>;

#[tauri::command]
pub async fn list_backups(service: CloudService) -> Result<Vec<BackupInfo>, String>;

#[tauri::command]
pub async fn set_backup_schedule(schedule: BackupSchedule) -> Result<(), String>;
```

### 2.12 앱 잠금 기능 상세 설계

#### 플랫폼별 잠금 방식

| 방식 | iOS | macOS | Windows | Android |
|------|-----|-------|---------|---------|
| **PIN (4-6자리)** | ✅ | ✅ | ✅ | ✅ |
| **Face ID** | ✅ | - | - | - |
| **Touch ID** | ✅ | ✅ | - | - |
| **Windows Hello** | - | - | ✅ | - |
| **지문인식** | - | - | - | ✅ |

#### 플랫폼별 보안 저장소

| 플랫폼 | 저장소 |
|--------|--------|
| **iOS** | Keychain Services |
| **macOS** | Keychain |
| **Windows** | Credential Manager |
| **Android** | Keystore |

#### PIN 잠금 구현

```rust
// PIN 저장 (bcrypt 해시)
use bcrypt::{hash, verify, DEFAULT_COST};

pub fn set_pin(pin: &str) -> Result<String, String> {
    hash(pin, DEFAULT_COST).map_err(|e| e.to_string())
}

pub fn verify_pin(pin: &str, hash: &str) -> bool {
    verify(pin, hash).unwrap_or(false)
}
```

- PIN 길이: 4-6자리
- 저장: bcrypt 해시 (cost=12)
- 저장 위치: 각 플랫폼별 보안 저장소 (keyring 라이브러리)

#### 생체인증 구현 (플랫폼별)

```rust
// iOS - Face ID / Touch ID
#[cfg(target_os = "ios")]
pub async fn authenticate_biometric() -> Result<bool, String> {
    // LocalAuthentication framework 호출
}

// macOS - Touch ID
#[cfg(target_os = "macos")]
pub async fn authenticate_biometric() -> Result<bool, String> {
    // LocalAuthentication framework 호출
}

// Windows - Windows Hello
#[cfg(target_os = "windows")]
pub async fn authenticate_biometric() -> Result<bool, String> {
    // Windows Hello API 호출
}

// Android - 지문인식
#[cfg(target_os = "android")]
pub async fn authenticate_biometric() -> Result<bool, String> {
    // BiometricPrompt API 호출
}
```

#### 자동 잠금 설정

| 설정 | 옵션 |
|------|------|
| **타이머** | 즉시 / 1분 / 5분 / 15분 / 30분 / 사용 안 함 |
| **잠금 시점** | 백그라운드 전환 시 / 앱 최소화 시 |

#### 잠금 해제 실패 처리

| 실패 횟수 | 조치 |
|----------|------|
| 1-3회 | 재시도 허용 |
| 4-5회 | 30초 대기 후 재시도 |
| 6회 이상 | 5분 대기 / 힌트 표시 |

#### 관련 Tauri Commands
```rust
#[tauri::command]
pub async fn set_lock_pin(pin: String) -> Result<(), String>;

#[tauri::command]
pub async fn verify_lock_pin(pin: String) -> Result<bool, String>;

#[tauri::command]
pub async fn authenticate_biometric() -> Result<bool, String>;

#[tauri::command]
pub async fn set_auto_lock_timeout(minutes: u32) -> Result<(), String>;

#[tauri::command]
pub async fn is_lock_enabled() -> Result<bool, String>;
```

---

## 3. 구현 계획

### 3.1 프로젝트 구조

```
sisters-salon-reservation-app/
├── src/                    # React 프론트엔드
│   ├── components/         # UI 컴포넌트 (기존 재사용)
│   │   ├── layout/             # 반응형 레이아웃
│   │   │   ├── DesktopLayout.tsx
│   │   │   ├── TabletLayout.tsx
│   │   │   ├── MobileLayout.tsx
│   │   │   └── ResponsiveContainer.tsx
│   │   └── navigation/         # 적응형 네비게이션
│   │       ├── Sidebar.tsx
│   │       ├── BottomTabs.tsx
│   │       └── CollapsibleSidebar.tsx
│   ├── contexts/           # 상태 관리
│   ├── hooks/              # 커스텀 훅
│   │   ├── useDeviceType.ts    # 디바이스 타입 감지
│   │   ├── useResponsive.ts    # 반응형 유틸
│   │   └── useCloudBackup.ts   # 클라우드 백업 훅
│   ├── lib/                # 유틸리티
│   │   └── tauri.ts        # Tauri API 래퍼
│   ├── types/              # TypeScript 타입
│   ├── App.tsx
│   └── main.tsx
├── src-tauri/              # Tauri 백엔드 (Rust)
│   ├── src/
│   │   ├── main.rs         # 메인 진입점
│   │   ├── commands/       # Tauri 커맨드
│   │   │   ├── reservations.rs
│   │   │   ├── designers.rs
│   │   │   ├── business_hours.rs
│   │   │   ├── statistics.rs
│   │   │   ├── export.rs       # Excel 내보내기
│   │   │   ├── backup.rs       # 클라우드 백업
│   │   │   └── security.rs     # 앱 잠금/인증
│   │   ├── db/             # 데이터베이스
│   │   └── services/       # 비즈니스 로직
│   │       ├── excel.rs
│   │       ├── cloud.rs
│   │       └── auth.rs
│   ├── gen/                # Tauri Mobile 생성 파일
│   │   ├── android/        # Android 프로젝트
│   │   └── apple/          # iOS/macOS 프로젝트
│   ├── Cargo.toml
│   └── tauri.conf.json     # 멀티 플랫폼 설정
├── docs/
│   └── analysis-report.md  # 이 문서
├── package.json
├── vite.config.ts
└── CLAUDE.md
```

### 3.2 마이그레이션 단계

#### Phase 1: 프로젝트 초기화 (2-3일)
- [ ] Tauri 2.0 + React + TypeScript 프로젝트 생성
- [ ] Tailwind CSS 설정 (반응형 브레이크포인트 포함)
- [ ] **Tauri iOS/Android 타겟 설정**
  - [ ] `npm run tauri ios init`
  - [ ] `npm run tauri android init`
- [ ] **반응형 레이아웃 컴포넌트 생성**
  - [ ] ResponsiveContainer, DesktopLayout, TabletLayout, MobileLayout
  - [ ] Sidebar, BottomTabs, CollapsibleSidebar

#### Phase 2: 데이터베이스 구축 (2-3일)
- [ ] Rust SQLite 설정 (rusqlite)
- [ ] 스키마 마이그레이션
- [ ] 기본 CRUD 커맨드 구현

#### Phase 3: 컴포넌트 마이그레이션 (4-5일)
- [ ] 예약 관리 컴포넌트 이전
- [ ] 디자이너 관리 컴포넌트 이전
- [ ] 영업시간 관리 컴포넌트 이전
- [ ] 통계 대시보드 이전
- [ ] API 호출 → Tauri invoke 변환
- [ ] **반응형 스타일 적용**
  - [ ] 모바일 카드 리스트 뷰
  - [ ] 태블릿 2컬럼 레이아웃
  - [ ] 데스크탑 3컬럼 레이아웃

#### Phase 4: 신규 기능 추가 (4-5일)
- [ ] Excel 내보내기 (rust_xlsxwriter)
  - [ ] 예약목록/월별통계/디자이너실적 시트
  - [ ] **모바일 공유 시트 연동**
- [ ] 클라우드 백업
  - [ ] iCloud 백업 (iOS/macOS)
  - [ ] Google Drive 연동 (Android/선택적)
  - [ ] 자동 백업 스케줄러
  - [ ] 백업 복원 기능
- [ ] 앱 잠금 기능
  - [ ] PIN 설정/해제
  - [ ] **생체인증 (플랫폼별)**
    - [ ] Face ID / Touch ID (iOS)
    - [ ] Touch ID (macOS)
    - [ ] Windows Hello
    - [ ] 지문인식 (Android)
  - [ ] 자동 잠금 타이머
- [ ] 앱 설정 화면

#### Phase 5: 테스트 및 빌드 (3-4일)
- [ ] 기능 테스트 (모든 플랫폼)
- [ ] **Desktop 빌드**
  - [ ] macOS (DMG, Universal Binary)
  - [ ] Windows (MSI)
  - [ ] Linux (DEB)
- [ ] **Mobile 빌드**
  - [ ] iOS 빌드 및 TestFlight 배포
  - [ ] Android 빌드 및 APK/AAB 생성

### 3.3 기술 의존성

#### Frontend (package.json)
```json
{
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@tauri-apps/api": "^2.0.0",
    "tailwindcss": "^3.4.0",
    "recharts": "^2.12.0",
    "react-calendar": "^5.0.0",
    "date-fns": "^3.0.0"
  }
}
```

#### Backend (Cargo.toml)
```toml
[dependencies]
# Core
tauri = { version = "2.0", features = ["sql"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
tokio = { version = "1.0", features = ["full"] }

# Database
rusqlite = { version = "0.31", features = ["bundled"] }

# Excel Export
rust_xlsxwriter = "0.79"

# Security - PIN/Credential Storage
keyring = "3.0"
bcrypt = "0.15"

# Cloud Backup (optional)
google-drive3 = { version = "5.0", optional = true }

# Biometric Auth (platform-specific)
[target.'cfg(target_os = "macos")'.dependencies]
objc = "0.2"
cocoa = "0.25"

[target.'cfg(target_os = "windows")'.dependencies]
windows = { version = "0.52", features = ["Security_Credentials_UI"] }

[features]
default = []
google-drive = ["google-drive3"]
```

---

## 4. 결론

### 4.1 전환 타당성

기존 프로젝트는 100% 완성된 상태로, 대부분의 프론트엔드 코드를 그대로 재사용할 수 있습니다. 주요 변경점은:

1. **백엔드**: Express.js → Tauri 2.0 (Rust)
2. **API 통신**: Axios HTTP → Tauri IPC
3. **인증**: JWT → 로컬 PIN + 생체인증 (선택)
4. **데이터베이스**: 서버 SQLite → 로컬 SQLite
5. **플랫폼**: Web → Desktop (macOS/Windows/Linux) + Mobile (iOS/Android)

### 4.2 예상 이점

- **멀티 플랫폼**: Desktop + Mobile 모두 지원
- **배포 용이성**: 각 플랫폼별 네이티브 앱으로 배포
- **오프라인 지원**: 인터넷 없이 완전한 기능
- **성능 향상**: 네트워크 지연 없음
- **보안 강화**: 데이터가 로컬에만 저장, 생체인증 지원
- **비용 절감**: 서버 운영 비용 없음
- **클라우드 백업**: iCloud/Google Drive로 데이터 동기화

### 4.3 권장 사항

**Tauri 2.0**을 사용한 크로스 플랫폼 앱 전환을 권장합니다. 기존 React 코드의 95%를 재사용하면서 Desktop과 Mobile 모두 지원하는 네이티브 앱을 제공할 수 있습니다.

---

**작성일**: 2026-01-21
**참조 프로젝트**: `/Users/minam.cho/workspaces/study/sisters-salon-reservation`
**지원 플랫폼**: macOS, Windows, Linux, iOS, Android
**예상 마이그레이션 기간**: 3-4주
